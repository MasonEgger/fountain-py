# Session Summary: Spec the 0.2.0 Release (Output Modes and Interchange)

**Date**: 2026-08-15
**Model**: claude-fable-5

## Key Actions

Scoped 0.2.0 into `spec.md` on branch `0.2.0-dev`, ready for the BPE cycle (review, plan, goal).

- **Found a shipped defect while reviewing**: `to_json()` raises `TypeError` on any document containing dual dialogue, because `to_dict()` passes metadata through verbatim and DUAL_DIALOGUE metadata holds live `FountainElement` objects. Probe-verified. Specced as F1 rather than a separate 0.1.1, per Mason's one-release ruling.
- **Wrote the 0.2.0 requirements section** continuing the 0.1.0 lettering: F (serialization fix, versioned JSON schema, `from_dict`/`from_json`), G (runtime-checkable `TextRenderer`/`BinaryRenderer` protocols), H (`PlainTextRenderer`), I (CLI with `validate` and `render` subcommands), J (FDX export, the XML industry tools actually consume), K (PDF as the `[pdf]` extra with geometry presets `LETTER`/`A4`/`HALF_LETTER` and data-driven layout profiles; booklet imposition pinned as a non-goal), L (docs how-tos and truth-up). Every group carries acceptance criteria written to be encoded as failing-first tests.
- **Recorded rejected candidates** in the scoped-futures section so the reasoning survives: generic XML (no audience; FDX covers it) and AsciiDoc (wrong audience; the renderer protocol is the escape hatch).
- **Added Open Questions 13 through 16** for Mason's review rulings: CLI executable name (rec: `fountain`), PDF dependency (rec: `fpdf2`), stage-play layout profile timing (rec: geometry now, profile later), FDX mapping for writer tools (rec: omit, matching HTML).
- Noted the Vale docs-lint gate in Available Tooling so plan sections can declare it as a linter.

## Notes

- On `0.2.0-dev` off `main`; nothing merged.
- Next: Mason runs `/bpe:review spec.md`, `/bpe:apply-review`, `/bpe:plan`, then `/bpe:goal full` and `/goal @goal.md`.
